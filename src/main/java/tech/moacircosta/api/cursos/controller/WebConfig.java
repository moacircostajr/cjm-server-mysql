package tech.moacircosta.api.cursos.controller;

//import org.springframework.context.annotation.Configuration;
//import org.springframework.web.servlet.config.annotation.CorsRegistry;
//import org.springframework.web.servlet.config.annotation.EnableWebMvc;
//import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

/* DEVE SER REMOVIDO AO ATIVAR O SPRING SECURITY */

//@Configuration
//@EnableWebMvc
public class WebConfig /*implements WebMvcConfigurer*/ {

    /*@Override
    public void addCorsMappings(CorsRegistry registry) {
        registry.addMapping("/**").allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS")
                .allowedOrigins("*").allowedHeaders("Access-Control-Allow-Origin", "Authorization", "Content-Type", "Accept", "Access-Control-Request-Headers", "Access-Control-Allow-Headers").allowCredentials(true).maxAge(3600);
    }*/
}
